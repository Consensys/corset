WHITESPACE = _{ " " | "\t" | "\r" | "\n" }
COMMENT = _{ ";" ~ (!NEWLINE ~ ANY)* }


corset = { SOI ~ constraint* ~ EOI }

constraint = { list }
list = { "(" ~ expr* ~ ")" }
expr = { integer | symbol | list }

symbol = @{ (LETTER | NUMBER | SYMBOL | "-" | "*" | "=" | "_" )+  }
integer = @{ ASCII_DIGIT+ }
